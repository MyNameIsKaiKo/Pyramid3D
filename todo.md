(re)ajouter les ennemies (looser)
ajouter les portes
ajouter les sauts et s accroupir

code en road map:

tableau de pointeut sur fonctions
While (il y a des fct à passer)
{
	if (fct > 0)
		return ;
	tab_fct++;
}